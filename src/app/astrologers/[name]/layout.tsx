// This is a new file created by App Prototyper to override the root layout
// and remove the main MobileNav from the astrologer profile page.

export default function AstrologerProfileLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
